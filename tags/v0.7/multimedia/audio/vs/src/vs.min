#-------------------------------------------------------------------------------
# VS (Voice Recognition)
#-------------------------------------------------------------------------------

VS_SOURCES=\
        vs.c 

VS_OBJS :=  $(addprefix $(TARGETDIR)/, $(VS_SOURCES:%.c=%.o))
VS_DEPS :=  $(addprefix $(TARGETDIR)/, $(VS_SOURCES:%.c=%.dep))

vpath %.c $(VS)
