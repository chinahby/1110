#-------------------------------------------------------------------------------
# CMX
#-------------------------------------------------------------------------------

CMX_SOURCES=\
        cmx.c
        
CMX_OBJS :=  $(addprefix $(TARGETDIR)/, $(CMX_SOURCES:%.c=%.o))
vpath %.c $(CMX)
