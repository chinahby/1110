#-------------------------------------------------------------------------------
# HW (hardware drivers)
#-------------------------------------------------------------------------------
HW_SOURCES=\
        hw.c \
	msm_init.c \
	hw_crash_debug.c \
        msmshadow.c

HW_OBJS :=  $(addprefix $(TARGETDIR)/, $(HW_SOURCES:%.c=%.o))

vpath %.c $(HW)

