/* 
===========================
Simulator Support DOCUMENTATION
============================
IHeap simulation

IHeap is partly simumated.

IHeap is simulated when BREW heap is being used. 

Heap tracking and memory leak detection is simulated when BREW heap is used.

BREW heap size specified in devicepack entry IDS_DD_HW_RAM_SIZE.

When devicepack with Windows heap setting is loaded,
memory tracking and heap validation features are
unavailable.
==========================  
*/
