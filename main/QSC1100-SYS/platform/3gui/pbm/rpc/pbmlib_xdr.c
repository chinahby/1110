/*=============================================================================
                            P B M L I B _ X D R . C

GENERAL DESCRIPTION
  This is an AUTO GENERATED file that provides xdr encode/decode routines
  specific to the data types used in the exported pbmlib API.

  ---------------------------------------------------------------------------
  Copyright (c) 2008 QUALCOMM Incorporated.
  All Rights Reserved. QUALCOMM Proprietary and Confidential.
  ---------------------------------------------------------------------------
=============================================================================*/

/*=============================================================================

                              Edit History

                             AUTO GENERATED                                  */
#ifdef FEATURE_HTORPC_METACOMMENTS
/* Generated by following versions of Htorpc modules:
Id: //depot/asic/msmshared/services/mproc/tools/main/htorpc/htorpc.pl#3 
Id: //depot/asic/msmshared/services/mproc/tools/main/htorpc/lib/Htorpc/Start.pm#7 
Id: //depot/asic/msmshared/services/mproc/tools/main/htorpc/lib/Htorpc/Htoxdr.pm#3 
Id: //depot/asic/msmshared/services/mproc/tools/main/htorpc/lib/Htorpc/XDR.pm#10 
Id: //depot/asic/msmshared/services/mproc/tools/main/htorpc/lib/Htorpc/Output.pm#16 
Id: //depot/asic/msmshared/services/mproc/tools/main/htorpc/lib/Htorpc/Parser.pm#8 
Id: //depot/asic/msmshared/services/mproc/tools/main/htorpc/lib/Htorpc/Metacomments.pm#6 
Id: //depot/asic/msmshared/services/mproc/tools/main/htorpc/lib/Htorpc/SymbolTable.pm#3  */
#endif
/* $Id: //depot/asic/msmshared/services/pbm/rpc/pbmlib_xdr.c#4 $
=============================================================================*/

#include "customer.h"
#include "target.h"

#ifdef FEATURE_ONCRPC
#ifdef FEATURE_EXPORT_PBMLIB
#define HTORPC_PBMLIB_GLUE

/* Include files */
#include "err.h"
#include "msg.h"
#include "oncrpc.h"

#include "pbmlib.h"
#include "pbmlib_rpc.h"


/*=======================================================================
                  XDR send/recv routines for API types
=======================================================================*/


/*
 * pbm_record_s_type XDR_PBMLIB_SEND
 */
boolean xdr_pbmlib_send_pbm_record_s_type (xdr_s_type *xdrs, pbm_record_s_type const *sptr)
{
  uint32 length_uint32;
  if ( ! XDR_SEND_INT( xdrs, &(sptr->index) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_BYTES(xdrs, sptr->number, ( 40 + 2 )) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_UINT8( xdrs, &(sptr->type) ) ) {
    return (FALSE);
  }

  length_uint32 = strlen((const char *) sptr->text) + 1;

  if ( ! XDR_SEND_UINT32( xdrs, &length_uint32 ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_STRING(xdrs, sptr->text, length_uint32) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_UINT32( xdrs, &(sptr->time) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_UINT32( xdrs, &(sptr->duration) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_UINT8( xdrs, &(sptr->is_answered) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_pbmlib_send_pbm_record_s_type */

/*
 * pbm_record_s_type XDR_PBMLIB_RECV
 */
boolean xdr_pbmlib_recv_pbm_record_s_type (xdr_s_type *xdrs, pbm_record_s_type *sptr)
{
  uint32 length_uint32;
  if ( ! XDR_RECV_INT( xdrs, &(sptr->index) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_BYTES(xdrs, sptr->number, ( 40 + 2 )) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_UINT8( xdrs, &(sptr->type) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_UINT32( xdrs, &length_uint32 ) ) {
    return (FALSE);
  }

  if ( length_uint32 > 0 ) {
    if ( length_uint32 > ( 64 + 1 ) ) {
      length_uint32 = ( 64 + 1 );
    }

    if ( ! XDR_RECV_STRING(xdrs, sptr->text, length_uint32) ) {
      return (FALSE);
    }
  }

  if ( ! XDR_RECV_UINT32( xdrs, &(sptr->time) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_UINT32( xdrs, &(sptr->duration) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_UINT8( xdrs, &(sptr->is_answered) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_pbmlib_recv_pbm_record_s_type */




/*
 * pbm_field_info_s_type XDR_PBMLIB_SEND
 */
boolean xdr_pbmlib_send_pbm_field_info_s_type (xdr_s_type *xdrs, pbm_field_info_s_type const *sptr)
{
  if ( ! XDR_SEND_UINT16( xdrs, &(sptr->field_id) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_INT( xdrs, &(sptr->num_items) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_INT( xdrs, &(sptr->max_length) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_UINT8( xdrs, &(sptr->field_type) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_UINT8( xdrs, &(sptr->field_search) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_UINT8( xdrs, &(sptr->field_cat_search) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_pbmlib_send_pbm_field_info_s_type */

/*
 * pbm_field_info_s_type XDR_PBMLIB_RECV
 */
boolean xdr_pbmlib_recv_pbm_field_info_s_type (xdr_s_type *xdrs, pbm_field_info_s_type *sptr)
{
  if ( ! XDR_RECV_UINT16( xdrs, &(sptr->field_id) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_INT( xdrs, &(sptr->num_items) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_INT( xdrs, &(sptr->max_length) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_UINT8( xdrs, &(sptr->field_type) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_UINT8( xdrs, &(sptr->field_search) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_UINT8( xdrs, &(sptr->field_cat_search) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_pbmlib_recv_pbm_field_info_s_type */




/*
 * pbm_event_data_u_type XDR_PBMLIB_SEND
 */
boolean xdr_pbmlib_send_pbm_event_data_u_type (xdr_s_type *xdrs, pbm_event_data_u_type const *uptr, pbm_notify_event_e_type obj_disc)
{
  if ( ( obj_disc == PBM_EVENT_REC_ADD ||
       obj_disc == PBM_EVENT_REC_UPDATE ||
       obj_disc == PBM_EVENT_REC_DELETE ||
       obj_disc == PBM_EVENT_REC_FAILED ) ) {

    if ( ! XDR_SEND_UINT16( xdrs, &(uptr->rec_id) ) ) {
      return (FALSE);
    }

  } else if ( ( obj_disc == PBM_EVENT_PB_REFRESH_START ||
              obj_disc == PBM_EVENT_PB_REFRESH_DONE ||
              obj_disc == PBM_EVENT_PB_READY ) ) {

    if ( ! XDR_SEND_ENUM( xdrs, &(uptr->pb_id) ) ) {
      return (FALSE);
    }

  } else if ( ( obj_disc == PBM_EVENT_LOCKED ||
              obj_disc == PBM_EVENT_UNLOCKED ) ) {

    if ( ! XDR_SEND_ENUM( xdrs, &(uptr->lock_type) ) ) {
      return (FALSE);
    }

  } else if ( ( obj_disc == PBM_EVENT_SIM_INIT_DONE ) ) {

    if ( ! XDR_SEND_UINT16( xdrs, &(uptr->sim_id) ) ) {
      return (FALSE);
    }

  } else {

    ERR_FATAL("xdr_pbmlib_send_pbm_event_data_u_type: can't switch on %d", obj_disc, 0, 0);

  }

  return (TRUE);
} /* xdr_pbmlib_send_pbm_event_data_u_type */

/*
 * pbm_event_data_u_type XDR_PBMLIB_RECV
 */
boolean xdr_pbmlib_recv_pbm_event_data_u_type (xdr_s_type *xdrs, pbm_event_data_u_type *uptr, pbm_notify_event_e_type obj_disc)
{
  if ( ( obj_disc == PBM_EVENT_REC_ADD ||
       obj_disc == PBM_EVENT_REC_UPDATE ||
       obj_disc == PBM_EVENT_REC_DELETE ||
       obj_disc == PBM_EVENT_REC_FAILED ) ) {

    if ( ! XDR_RECV_UINT16( xdrs, &(uptr->rec_id) ) ) {
      return (FALSE);
    }

  } else if ( ( obj_disc == PBM_EVENT_PB_REFRESH_START ||
              obj_disc == PBM_EVENT_PB_REFRESH_DONE ||
              obj_disc == PBM_EVENT_PB_READY ) ) {

    if ( ! XDR_RECV_ENUM( xdrs, &(uptr->pb_id) ) ) {
      return (FALSE);
    }

  } else if ( ( obj_disc == PBM_EVENT_LOCKED ||
              obj_disc == PBM_EVENT_UNLOCKED ) ) {

    if ( ! XDR_RECV_ENUM( xdrs, &(uptr->lock_type) ) ) {
      return (FALSE);
    }

  } else if ( ( obj_disc == PBM_EVENT_SIM_INIT_DONE ) ) {

    if ( ! XDR_RECV_UINT16( xdrs, &(uptr->sim_id) ) ) {
      return (FALSE);
    }

  } else {

    ERR_FATAL("xdr_pbmlib_recv_pbm_event_data_u_type: can't switch on %d", obj_disc, 0, 0);

  }

  return (TRUE);
} /* xdr_pbmlib_recv_pbm_event_data_u_type */




/*
 * pbm_notify_data_s_type XDR_PBMLIB_SEND
 */
boolean xdr_pbmlib_send_pbm_notify_data_s_type (xdr_s_type *xdrs, pbm_notify_data_s_type const *sptr)
{
  if ( ! XDR_SEND_ENUM( xdrs, &(sptr->event) ) ) {
    return (FALSE);
  }

  if ( ! xdr_pbmlib_send_pbm_event_data_u_type( xdrs, &(sptr->data), sptr->event ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_pbmlib_send_pbm_notify_data_s_type */

/*
 * pbm_notify_data_s_type XDR_PBMLIB_RECV
 */
boolean xdr_pbmlib_recv_pbm_notify_data_s_type (xdr_s_type *xdrs, pbm_notify_data_s_type *sptr)
{
  if ( ! XDR_RECV_ENUM( xdrs, &(sptr->event) ) ) {
    return (FALSE);
  }

  if ( ! xdr_pbmlib_recv_pbm_event_data_u_type( xdrs, &(sptr->data), sptr->event ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_pbmlib_recv_pbm_notify_data_s_type */




/*
 * pbm_writecb_data_s_type XDR_PBMLIB_SEND
 */
boolean xdr_pbmlib_send_pbm_writecb_data_s_type (xdr_s_type *xdrs, pbm_writecb_data_s_type const *sptr)
{
  if ( ! XDR_SEND_ENUM( xdrs, &(sptr->ret) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_UINT16( xdrs, &(sptr->rec_id) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_HANDLE( xdrs, &(sptr->user_data) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_pbmlib_send_pbm_writecb_data_s_type */

/*
 * pbm_writecb_data_s_type XDR_PBMLIB_RECV
 */
boolean xdr_pbmlib_recv_pbm_writecb_data_s_type (xdr_s_type *xdrs, pbm_writecb_data_s_type *sptr)
{
  if ( ! XDR_RECV_ENUM( xdrs, &(sptr->ret) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_UINT16( xdrs, &(sptr->rec_id) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_HANDLE( xdrs, &(sptr->user_data) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_pbmlib_recv_pbm_writecb_data_s_type */




/*
 * pbm_extended_fileinfo_s_type XDR_PBMLIB_SEND
 */
boolean xdr_pbmlib_send_pbm_extended_fileinfo_s_type (xdr_s_type *xdrs, pbm_extended_fileinfo_s_type const *sptr)
{
  if ( ! XDR_SEND_INT( xdrs, &(sptr->records_used) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_INT( xdrs, &(sptr->number_of_records) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_INT( xdrs, &(sptr->text_len) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_INT( xdrs, &(sptr->number_len) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_pbmlib_send_pbm_extended_fileinfo_s_type */

/*
 * pbm_extended_fileinfo_s_type XDR_PBMLIB_RECV
 */
boolean xdr_pbmlib_recv_pbm_extended_fileinfo_s_type (xdr_s_type *xdrs, pbm_extended_fileinfo_s_type *sptr)
{
  if ( ! XDR_RECV_INT( xdrs, &(sptr->records_used) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_INT( xdrs, &(sptr->number_of_records) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_INT( xdrs, &(sptr->text_len) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_INT( xdrs, &(sptr->number_len) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_pbmlib_recv_pbm_extended_fileinfo_s_type */




#endif /* FEATURE_EXPORT_PBMLIB */
#endif /* FEATURE_ONCRPC */
