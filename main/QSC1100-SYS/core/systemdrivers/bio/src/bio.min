#-------------------------------------------------------------------------------
# BIO (boot I/O services)
#-------------------------------------------------------------------------------

BIO_SOURCES=\
        bio.c

BIO_OBJS :=  $(addprefix $(TARGETDIR)/, $(BIO_SOURCES:%.c=%.o))
BIO_DEPS :=  $(addprefix $(TARGETDIR)/, $(BIO_SOURCES:%.c=%.dep))
vpath %.c $(BIO)
