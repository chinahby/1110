#-------------------------------------------------------------------------------
# Serial Peripheral Interface (SPI)
# $Header: //depot/asic/msmshared/users/wxie/spi/q1110/spi/source/spi.min#2 $
#-------------------------------------------------------------------------------

SPI_C_SOURCES=spi.c HALspi.c

SPI_OBJS := $(addprefix $(TARGETDIR)/, $(SPI_C_SOURCES:%.c=%.o))

$(SPI_OBJS): CFLAGS := $(CFLAGS) $(HAL_INCLUDES)

vpath %.c $(SPI)
vpath %.c $(SPI)/common
vpath %.s $(SPI)

