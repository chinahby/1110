#-------------------------------------------------------------------------------
# PROGRESSIVE BOOT (boot code)
#-------------------------------------------------------------------------------

BOOT_PROGRESSIVE_SOURCES=\
        boot_bad_block_list.c \
        boot_read_pages.c \
	pboot_genhash.c \
	boot_pager.c
	
BOOT_PROGRESSIVE_OBJS :=  $(addprefix $(TARGETDIR)/, $(BOOT_PROGRESSIVE_SOURCES:%.c=%.o))
vpath %.c $(BOOT) 


